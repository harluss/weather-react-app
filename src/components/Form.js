import Input from 'components/Input';

const Form = () => {
  return (
    <div>
      <Input label="city" id="city" name="city" />
      <Input label="country" id="country" name="country" />
    </div>
  );
};

export default Form;
